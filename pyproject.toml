[project]
name = "ielts-speak"
version = "0.1.0"
description = "IELTS speaking random quiz: TTS questions with exam-room countdown clock"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.1",
]
